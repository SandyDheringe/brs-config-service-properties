service:
  name: brs-admin-service

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/bus_reservation_db
    password: root
  jpa:
    hibernate:
      ddl-auto: update
